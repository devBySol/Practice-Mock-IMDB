@model IEnumerable<Movie>

@{
    ViewData["Title"] = "Home";
}

<div class="position-absolute container bg-light text-dark p-4 start-50 translate-middle-x">
    <div class="row">
        <main class="col-12">
            <nav class="position-fixed w-100 d-flex justify-content-between align-content-center  py-2 px-5 rounded-1"
         
                 style="top: -50px; left: 50%; transform: translateX(-50%); z-index: 1030; background-color: rgba(255, 255, 255, 0.9);">
                <ul class="nav" id="navTabs">
                    <li class="nav-item"><a href="#" class="nav-link text-dark">IN THEATERS</a></li>
                    <li class="nav-item"><a href="#" class="nav-link text-dark">COMING SOON</a></li>
                    <li class="nav-item"><a href="#" class="nav-link text-dark">CHARTS</a></li>
                    <li class="nav-item"><a href="#" class="nav-link text-dark">TV SERIES</a></li>
                    <li class="nav-item"><a href="#" class="nav-link text-dark">TRAILERS</a></li>
                
                </ul>
                <button class="btn btn-warning ms-3 text-white"><i class="bi bi-star-fill"> @Model.Count() </i></button>
            </nav>



            @* Movie Grid *@
            <div class="container mt-4">
                <div class="row">
                    @foreach (var movie in Model)
                    {
                        <div class="col-lg-4 col-md-6 col-sm-12 mb-4">
                            <div class="card shadow-sm border-0 rounded">
                                <img src="@movie.PosterUrl" class="card-img-top" style="height: 400px; object-fit: contain;" alt="@movie.Title Poster">
                                <div class="card-body text-center">
                                    <h6 class="fw-bold">@movie.Title</h6>
                                    <p class="text-secondary">@movie.Genre</p>
                                    <div class="d-flex justify-content-between align-items-center">
                                        <span class="text-warning fw-bold"><i class="bi bi-star-fill"> @movie.Rating</i></span>
                                        <a asp-controller="Movie" asp-action="Details" asp-route-id="@movie.Id" class="btn btn-outline-warning btn-sm">View</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>

            @* Load More Button *@
            <div class="text-center mt-4">
                <button class="btn btn-warning rounded-circle text-white fs-">⋯</button>
            </div>
        </main>
    </div>
</div>


<script>
    document.addEventListener("DOMContentLoaded", function() {
        let navLinks = document.querySelectorAll("#navTabs .nav-link");

        navLinks.forEach(link => {
            link.addEventListener("click", function() {

                navLinks.forEach(l => {
                    l.classList.remove("text-warning", "active");
                    l.classList.add("text-dark");
                });


                this.classList.remove("text-dark");
                this.classList.add("text-warning",  "active");
            });
        });
    });
</script>

